title: "André Barbosa de Amaral"
email: bcn.andre@gmail.com
baseurl: ""
url: "https://heyvoon.github.io"
remote_theme: pages-themes/hacker@v0.2.0
plugins:
  - jekyll-remote-theme

include:
  - assets/resumes

defaults:
  - scope:
      path: "assets/resumes"
    values:
      permalink: /resume-:basename/

# Resume configuration is handled by the defaults section above
# The resume files are now in assets/resumes/ with proper permalinks